# PetrovBit

Indie game studio focused on retro-style mobile games.

Coming soon.

— Mikhail Petrov

## TODO

- [x] Общая оболочка PetrovBit переведена с плоского 8-битного кирпича Tank 1990 на материальный язык Pixel Tanks. Главная сохраняет единую сетку и компоненты, а три карточки используют локальные игровые диалекты. Решение и границы описаны в `CLAUDE.md`.
- [ ] Обновить ссылки на Google Play / RuStore для Tank 1990 (сейчас ведут на старый пакет `io.battle_city.game`), когда зальём новые APK с пакетами `dev.petrovbit.*`. Ссылки прописаны в `v2/shared.jsx` (`PB_STORE_LINKS.tank`).
- [ ] Заменить заглушки соцсетей в подвале. `telegram` и `youtube` в `PB_CONTENT.footer.links` (`v2/shared.jsx`, обе локали — `en` и `ru`) до сих пор стоят как `"#"` и никуда не ведут. У Tank 1990 уже есть ролик: https://youtu.be/3t_NmDFEvg0 — можно поставить его или канал целиком.
- [ ] Проверить ссылку Google Play на Pixel Tanks. Она уже прописана в `PB_STORE_LINKS.pix` (`dev.petrovbit.pixeltanks`), но карточка отдаёт 404, пока не пройдёт модерация — как пройдёт, бейдж оживёт сам, отдельной правки не нужно, только сверить.
- [ ] Плашка-уведомление про cookies. Сайт использует Яндекс.Метрику (счётчик в `v2/metrika.js`), в политике конфиденциальности это описано, но уведомления на самих страницах нет. Пока плашки нет — вебвизор держим выключенным (`YM_WEBVISOR = false` в `v2/metrika.js`): запись сессий без явного уведомления включать не стоит. Как появится плашка — можно включить.
